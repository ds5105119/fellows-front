import { UserERPNextProject } from "@/@types/service/project";

export function TeamsList({ project }: { project: UserERPNextProject }) {
  return (
    <div>
      <h1>Teams List</h1>
      <p>This is the Teams List page.</p>
    </div>
  );
}
